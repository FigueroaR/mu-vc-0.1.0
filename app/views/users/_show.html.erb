
<div align="center">
  <img  style='width: 500px' src="<%=@user.image%>">
  <div>
    <%= image_tag(@user.photo) if @user.photo.attached? %>
  </div>
</div>
<h1 align="center"> <%=  @user.name %></h1>
<hr>
<p>
  <h3>Upcoming Rsvps</h3>
    <% @coming.all.each do |e| %>
      <li><%= link_to e.name, event_path(e)%><br></li>
    <%end%>
    
  <h3>Past Rsvps</h3>
    <% @past.all.each do |e| %>
      <li><%= link_to e.name, event_path(e)%><br></li>
    <%end%>
</p>

